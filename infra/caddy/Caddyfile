{
        email {$LETSENCRYPT_EMAIL}
}

{$APP_DOMAIN} {
        encode zstd gzip

        handle_path /api/* {
                reverse_proxy server:3000
        }

        handle {
                reverse_proxy client:3001
        }
}